# 列名错误修复

## 诊断结果
SQL 使用了不存在的列名，数据库实际列名已列出。

{actual_columns}

错误列名 "{wrong_col}" 的建议替换: {suggested_col}

## 修复步骤
1. 将 SQL 中所有不存在的列名，替换为上面对应的实际列名
2. 检查替换后的 SQL 是否语法正确（如表别名是否限定）
3. 只输出修正后的 SQLite SQL，不要解释
