# 语义缺口修复

## 诊断结果
SQL 语法正确，但未覆盖以下需求项。

## 缺口清单
{gap_list_text}

## 原始查询
{query}

## 当前 SQL
```sql
{sql}
```

## 表结构
{schema_context}

{lookup_context}

## 核心约束
{core_rules}

## 修复步骤
1. 仔细阅读缺口清单中的每一项，理解缺失的需求
2. 在不破坏 SQL 已有正确部分的前提下，逐一补全缺失的子句
3. 如果缺口涉及子查询（如计算平均值），使用 CTE（WITH 子句）组织
4. 如果缺口涉及条件标记（如是/否判断），使用 CASE WHEN
5. 补全完毕后自查：所有原有正确逻辑是否保留、新增逻辑是否正确

只输出修正后的 SQLite SQL，不要解释。
